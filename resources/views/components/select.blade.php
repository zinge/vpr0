<div class="form-group row">
  <label for="{{$compId}}_id" class="col-form-label">{{$compLabel}}:</label>
  <div>
    <select multiple class="form-control" name="{{$compId}}_id" id="{{$compId}}_id">
      {{-- this need correct--}}

    </select>
  </div>
</div>
