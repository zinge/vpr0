<div class="form-group row">
  <label for="{{$compId}}" class="col-form-label">{{$compLabel}}:</label>
  <div>
    <input type="text" name="{{$compId}}" id="{{$compId}}" class="form-control">
  </div>
</div>
